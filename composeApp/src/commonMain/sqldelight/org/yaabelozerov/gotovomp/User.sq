CREATE TABLE User (
    username TEXT NOT NULL PRIMARY KEY,
    firstName TEXT,
    lastName TEXT,
    phoneNumber TEXT
);

-- User queries
insertUser:
INSERT INTO User(username)
VALUES (?);

selectUserByUsername:
SELECT * FROM User
WHERE username = ?;